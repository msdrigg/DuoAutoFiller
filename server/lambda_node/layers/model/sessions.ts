type FrontendSession = {
    id: string,
    key: string,
    context: SessionContext,
    expiration: Date,
}

interface SessionContext {
    name: string
}

interface SessionContext {
    name: string
}