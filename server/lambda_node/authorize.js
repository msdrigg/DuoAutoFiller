async function sessionAuthorizer(event, context) {
    // Authorize session based off session existing
}

async function userAuthorizer(event, context) {
    // Authorize user based off Authorization cookie with email (b64) and password
}